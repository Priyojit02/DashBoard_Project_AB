# ============================================
# ENVIRONMENT CONFIGURATION - Development
# ============================================

# Application
APP_NAME=SAP Ticket Management System
APP_ENV=development
DEBUG=true
SECRET_KEY=dev-secret-key-change-in-production-abc123
API_VERSION=v1
ALLOWED_HOSTS=http://localhost:3000,http://127.0.0.1:3000

# Database - Supabase PostgreSQL (Direct connection)
DB_HOST=db.mejhzlbfiptlcsawplzw.supabase.co
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=70070012@PJkabcd

# Azure AD - SSO Authentication (Microsoft Login)
AZURE_CLIENT_ID=
AZURE_TENANT_ID=

# Email - Fetched via Microsoft Graph API using SSO token
# No password needed - uses Azure AD authentication
EMAIL_FOLDER=INBOX
EMAIL_FETCH_DAYS=1

# ============================================
# LLM Configuration - Simple Dynamic Setup
# ============================================
# Just change provider name and API key - that's it!

# Provider: openai | anthropic | azure | ollama | groq | together | google
LLM_PROVIDER=openai
LLM_MODEL=gpt-4-turbo-preview
LLM_API_KEY=sk-your-api-key-here
LLM_BASE_URL=
LLM_TEMPERATURE=0.3
LLM_MAX_TOKENS=1000

# Azure OpenAI only (if LLM_PROVIDER=azure)
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_DEPLOYMENT=
AZURE_OPENAI_API_VERSION=2024-02-15-preview

# Logging
LOG_LEVEL=DEBUG
